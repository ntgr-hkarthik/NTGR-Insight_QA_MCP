# Cursor project config

Editor-local **Cursor** settings for this repository.

| Path | Role |
|------|------|
| **`hooks/`** | Optional hook scripts and `state/` JSON — see **`hooks/README.md`** |

Not required to **run** the dashboard, Playwright, or the MCP server — only for Cursor-specific automation if enabled on your machine.
