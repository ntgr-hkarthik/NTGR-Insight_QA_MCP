# NTGR-Insight_QA assets

Static files bundled with the MCP package (for example the Insight logo used in **`README.md`**).

Safe to replace with updated branding as long as paths referenced in documentation stay valid.
