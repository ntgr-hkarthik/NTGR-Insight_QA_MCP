# Dashboard history

Stores **archived JSON snapshots** of completed Playwright runs (one file per run, written by `dashboard/server.js`).

Safe to delete for a clean slate; nothing in the test runner depends on these files for correctness.
