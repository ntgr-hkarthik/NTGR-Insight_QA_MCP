# Account Navigator — static UI

Browser-facing assets for the navigator: **HTML**, **CSS**, and client **JavaScript** (including `env-manager.js` shared with the dashboard pattern).

Served by `tools/account-navigator/server.js` as static files. Edit here for labels, layout, and client-side behavior; API changes belong in `server.js`.
